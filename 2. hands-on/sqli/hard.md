0. Go to elements and change option value. Use Burp to intercept HTTP inspect

hard version consists of two steps 
- POST /vulnerabilities/sqli/session-input.php HTTP/1.1
- GET /vulnerabilities/sqli/ HTTP/1.1

The rest is same as easy version
